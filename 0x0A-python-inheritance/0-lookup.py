#!/usr/bin/python3
"""
This is module 0-lookup
This modules supplies the lookup(obj)
"""


def lookup(obj):
    """Returns the list of all available attributes and methods of an object"""
    return (dir(obj))
